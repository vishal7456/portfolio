{
  "name": "backend",
  "version": "1.0.0",
  "description": "Node.js backend for portfolio project",
  "main": "index.js",
  "scripts": {
    "start": "node index.js",
    "build":"npm install && npm install --prefix frontend &&npm run build --prefix frontend"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "dependencies": {
    "express-rate-limit": "^8.0.1"
  }
}
